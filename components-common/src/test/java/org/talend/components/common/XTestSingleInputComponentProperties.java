// ============================================================================
//
// Copyright (C) 2006-2016 Talend Inc. - www.talend.com
//
// This source code is available under agreement available at
// %InstallDIR%\features\org.talend.rcp.branding.%PRODUCTNAME%\%PRODUCTNAME%license.txt
//
// You should have received a copy of the agreement
// along with this program; if not, write to Talend SA
// 9 rue Pages 92150 Suresnes, France
//
// ============================================================================
package org.talend.components.common;

import org.junit.Test;

public class XTestSingleInputComponentProperties {

    /**
     * Test method for
     * {@link org.talend.components.common.SingleInputComponentProperties#getAvailableConnectors(java.util.Set, boolean)}
     * .
     */
    @Test
    public void testGetAvailableConnections() {
        // SingleInputComponentProperties singleInputComponentProperties = new SingleInputComponentProperties("foo");
        // Set<String> availableConnections = singleInputComponentProperties.getAvailableConnections(null, false);
        // assertThat(availableConnections, hasSize(1));
        // availableConnections = singleInputComponentProperties.getAvailableConnections(Collections.EMPTY_SET, false);
        // assertThat(availableConnections, hasSize(1));
        // availableConnections =
        // singleInputComponentProperties.getAvailableConnections(Collections.singleton("mainInput"), false);
        // assertThat(availableConnections, hasSize(0));
        // availableConnections = singleInputComponentProperties.getAvailableConnections(null, true);
        // assertThat(availableConnections, hasSize(0));
        // availableConnections = singleInputComponentProperties.getAvailableConnections(Collections.EMPTY_SET, true);
        // assertThat(availableConnections, hasSize(0));
        // availableConnections =
        // singleInputComponentProperties.getAvailableConnections(Collections.singleton("mainInput"), true);
        // assertThat(availableConnections, hasSize(0));
    }

}
